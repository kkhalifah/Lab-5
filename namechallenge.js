var name1 ="Jenniferr";
var name2 = " Hunter";
var name3 = "Karen";

if (name1.length > name2.length && name1.length > name3.length) {
  console.log(name1 + " has longer name")
}
else if (name2.length > name3.length && name2.length > name1.length) {
  console.log(name2 + "has longer name")
}
else { console.log (name3 + " has longer name")
}
